// Use .innerHTML to render a Buy! button inside the div container
const buyBtn = document.getElementById("container")
buyBtn.innerHTML = "<button>Buy!</button>" // without button will print plain "Buy!"

